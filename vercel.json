{
  "buildCommand": "npm run build:demo && cp public/sitemap.xml dist/ && cp public/robots.txt dist/",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
